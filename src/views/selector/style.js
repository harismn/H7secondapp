import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    focus: {
        fontSize : 15,
        color: 'white',
        padding: 15,  
    },
    blur: {
        fontSize : 15,
        color: '#E0E0E0',
        padding: 15,  
    },
});  

export default styles;